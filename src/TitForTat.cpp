#include "TitForTat.h"

TitForTat::TitForTat(int number) : Player(number)
{
    mad=false;
}
void TitForTat::decision()
{
    if(!mad) //if not mad
    {
        // trust otherperson
    }
    else //mad
    {
        // retaliate
        // set mad= to false
    }
}
